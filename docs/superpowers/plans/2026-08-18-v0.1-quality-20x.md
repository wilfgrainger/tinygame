# TinyWorld V0.1 20x Quality Pass Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the existing V0.1 slice dramatically better to control, explore and inhabit on Android without adding new feature families.

**Architecture:** Keep the existing PlayCanvas/Vite/Worker architecture. Move quality logic into small pure helpers or focused render modules, preserve current persistence/API contracts, and use TDD for behavioural changes. Human visual/gameplay acceptance remains mandatory.

**Tech Stack:** TypeScript 7, Vite 8, PlayCanvas 2.21, Vitest 4, Playwright 1.61, Cloudflare Worker + D1.

**Spec:** `docs/superpowers/specs/2026-08-18-v0.1-quality-20x-design.md`

**Frozen code candidate:** `c3286c7d438d8ac0a79e5f8db9bbc22a8da59f75`  
**Final code CI:** run #117 — PASS

## Global Constraints

- No new feature families, jobs, shops, vehicles, missions, multiplayer, economy or persistence domains.
- Android landscape is the primary acceptance target; portrait remains mechanically correct.
- Left stick is camera-relative; moving avatar faces travel direction; right drag controls camera independently.
- Roads/paths remain terrain-following and traversal limits remain enforced.
- Keep Cloudflare Free-plan architecture and current distribution budget.
- No per-frame network traffic.
- Real Android and family acceptance override CI.

---

### Task 1: Lock the simple third-person control contract — COMPLETE

**Files:**
- `src/game/player/controlModel.ts`
- `src/game/player/PlayerController.ts`
- `src/game/player/PlayerView.ts`
- `tests/unit/control-model.test.ts`
- `tests/unit/player-controller.test.ts`

**Interfaces:**
- `cameraRelativeMove(moveX, moveY, cameraYaw)` returns normalized world-space planar input.
- `applyCameraLook(camera, lookX, lookY)` changes camera angles only.
- `facingYawForMovement(move, fallbackYaw)` points the avatar toward travel.

- [x] Write RED tests for camera-relative movement, independent camera look and movement-facing avatar.
- [x] Confirm RED on CI.
- [x] Implement minimal control model and visual-facing separation.
- [x] Tighten pitch-limit assertions to `-38..12`.
- [x] Add joystick deadzone coverage.
- [x] Confirm exact-head full CI GREEN.

### Task 2: Improve camera and walking feel without adding modes — COMPLETE

**Files:**
- `src/game/player/movementFeel.ts`
- `src/game/player/cameraRig.ts`
- `src/game/player/PlayerController.ts`
- `src/game/app/GameApp.ts`
- `tests/unit/movement-feel.test.ts`
- `tests/unit/camera-rig.test.ts`

**Interfaces:**
- `applyRadialDeadzone(x, y, deadzone = 0.12)` preserves direction and remaps magnitude.
- `approachMagnitude(current, target, dt, acceleration, deceleration)` gives bounded responsive speed response.
- `cameraTargetInto(out, position, yaw, pitch, aspect, inVehicle)` updates an allocation-free camera target.

- [x] Write failing tests proving tiny joystick noise maps to zero, moderate input remains responsive, and release decelerates faster than acceleration.
- [x] Confirm RED.
- [x] Implement pure movement helpers and integrate into on-foot/swim movement.
- [x] Write failing camera tests for landscape/portrait framing and frame-rate-independent damping.
- [x] Implement pure camera target calculation and reuse preallocated PlayCanvas vectors.
- [x] Drive on-foot animation from actual travel rather than raw input magnitude.
- [x] Run full CI and commit.

### Task 3: Make Tiny Bike and Tiny Raft feel physical — COMPLETE

**Files:**
- `src/game/vehicles/vehicleFeel.ts`
- `src/game/vehicles/RaftWake.ts`
- `src/game/vehicles/BikeController.ts`
- `src/game/vehicles/RaftController.ts`
- `src/game/app/GameApp.ts`
- `tests/unit/bike.test.ts`
- `tests/unit/raft.test.ts`
- `tests/unit/vehicle-feel.test.ts`

**Interfaces:**
- `steerResponse(input, speedRatio, low, high)` returns progressive steering rate.
- `bikeLeanDegrees(steerInput, speedRatio)` returns bounded visual lean.
- `raftPose(time, speedRatio, steerInput)` returns lightweight bob/roll/pitch values.
- `wakeStrength(speedRatio)` returns `0..1` for render feedback.

- [x] Write RED tests for progressive steering, bounded bike lean and raft response.
- [x] Confirm RED.
- [x] Implement progressive bike steering without changing mount/dismount contracts.
- [x] Implement progressive raft steering and bounded water pose.
- [x] Add visible speed-dependent bike lean.
- [x] Add lightweight reusable raft wake/ripple presentation.
- [x] Use wider vehicle camera framing for bike/raft/car.
- [x] Run full CI and commit.

### Task 4: Add authored quality scenery as a focused module — COMPLETE

**Files:**
- `src/game/world/QualityScenery.ts`
- `src/game/world/qualityComposition.ts`
- `src/game/world/Atmosphere.ts`
- `tests/unit/quality-composition.test.ts`

**Interfaces:**
- `QUALITY_COMPOSITION` contains deterministic decorative clusters for Woodland, Mountain, Harbour and Home Lane.
- `QualityScenery.build()` renders non-colliding scenery only; it does not change persistence, routes or gameplay systems.

- [x] Write RED data tests requiring all four outer zones to have multiple authored clusters and one anchor.
- [x] Confirm RED.
- [x] Add deterministic composition data with open routes and no uniform random scattering.
- [x] Render Woodland threshold/grove framing.
- [x] Render Mountain approach/summit cairn/lookout framing.
- [x] Render Harbour arrival/mooring/shoreline framing.
- [x] Render Home Lane garden/lantern/bench/boundary warmth.
- [x] Keep this layer decorative and non-colliding.
- [x] Run full CI and commit.

### Task 5: Reduce ambient allocation and mobile render risk — COMPLETE

**Files:**
- `src/game/world/Atmosphere.ts`
- `src/game/world/particlePool.ts`
- `src/game/app/GameApp.ts`
- `tests/unit/atmosphere-budget.test.ts`

**Interfaces:**
- Atmosphere maintains bounded reusable particle pools rather than create/destroy churn in normal updates.
- Mobile shadow map is capped at 1024 for this candidate.

- [x] Write RED behaviour test proving fixed pools are bounded and reusable.
- [x] Confirm RED.
- [x] Preallocate/reuse smoke and fountain entities instead of routine destroy/create.
- [x] Change smoke/fountain cadence from frame-probability-driven to time-driven.
- [x] Reduce shadow resolution from 2048 to 1024.
- [x] Remove per-frame camera `pc.Vec3` allocation.
- [x] Run full CI and commit.

### Task 6: Release handoff and exact-build acceptance — ENGINEERING COMPLETE / HUMAN GATES OPEN

**Files:**
- `progress.md`
- `docs/quality/v0.1-android-acceptance.md`

- [x] Record last code-verified gameplay SHA separately from later documentation commits.
- [x] Keep DEV deployment status explicit rather than assuming old DEV equals new branch.
- [x] Run full CI on final code candidate (`c3286c7d...`, run #117 PASS).
- [ ] Deploy exact candidate to Cloudflare DEV using an authenticated owner environment.
- [ ] Verify `/release.json` and visible build stamp both identify `c3286c7d...`.
- [ ] Owner performs Android landscape control/camera route and 10-minute quality/performance test.
- [ ] Perform portrait mechanical sanity check because original defect evidence was portrait.
- [ ] Family testers independently identify three enjoyable activities and record PASS/FAIL.
- [ ] Only after both human gates PASS, mark PR #4 ready for merge.

## Completion Gate

The **code-owned 20x implementation is complete** at `c3286c7d438d8ac0a79e5f8db9bbc22a8da59f75`.

The release is **not accepted** until the exact deployed candidate satisfies:

- Android acceptance;
- family acceptance.

A green CI run does not prove visual quality, device feel or fun.
